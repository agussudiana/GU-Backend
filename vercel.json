{
    "version":2,
    "builds":[
        {
            "src":"dist/index.js",
            "use":"@vercel/node",
            "config": { "excludeFiles": ["public"] }
        }
    ],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"dist/index.js"
        }
    ]
}